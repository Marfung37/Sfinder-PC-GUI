# Board

# Saves
General Saves = False # gives the general saves of each piece and duplicates
Over PC Cases = False # gives the percents of saves over the total cases which are solveable over default of over total permutations of pieces
Save Fraction = True # give the percents with the (cases/total)
Write Fails = False # puts the fails queues for the saves in the saveOutput file

# Solves

# Percent